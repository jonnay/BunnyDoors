import org.getspout.spoutapi.event.inventory.InventoryOpenEvent;
import org.getspout.spoutapi.event.inventory.InventoryCloseEvent;


public class SpoutKey {
    /*

GenericPopup somePopup = new GenericPopup();
GenericLabel label = new GenericLabel("This is a label");
somePopup.attachWidget(plugin, label);
SpoutPlayer.getMainScreen().attachPopupScreen(somePopup);

GenericTexture texture = new GenericTexture();
texture.setUrl("http://example.com/texture.png");
texture.setWidth(64).setHeight(64); // The size as the downloaded image.
	*/
}
